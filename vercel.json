{
    "build": {
        "env": {
            "PNPM_INSTALL_FLAGS": "--no-frozen-lockfile"
        }
    },
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}